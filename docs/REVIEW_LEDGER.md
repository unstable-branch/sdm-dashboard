# Astra review ledger

**Source review:** 2026-09-15, exact main snapshot b39f3a4ef88c7551df29f1f7b3e93d4b32034cec.
**Ledger snapshot:** 2026-09-17, documenting the accepted recovery dispositions and the remaining evidence gates.

Statuses are honest source/evidence dispositions, not release approval. Implemented means a focused source contract exists; acceptance still requires the named gate. Every finding is mapped to a future dev-targeted implementation PR and eventual exact-SHA release review.

| ID | Status at this snapshot | Subsystem and evidence | Next acceptance gate | Eventual implementation / review |
| --- | --- | --- | --- | --- |
| P1-01 | **Partial** | Hono/queue model paths resolve canonical occurrence asset IDs. Shared inputs and direct Plumber parity for boundary, mask, target-group, and climate paths remain open. | Two-user foreign/ownerless/revoked/deleted/traversal matrix; zero reads/spawns on denial. | security/canonical-inputs PR to dev; release PR verifies all input kinds. |
| P1-02 | **Partial** | Status/error authorization ordering was moved ahead of parsed failure data; fail-closed handling for unreadable, ownerless, or ambiguous metadata remains to be completed and exercised. | Direct Plumber failed/cancelled/corrupt/legacy records with metadata-disclosure assertions and side-effect checks. | security/authorization-boundaries PR to dev; exact-SHA security matrix. |
| P1-03 | **Implemented, integration pending** | Current-principal Plumber clients and forwarded user/role validation exist in Hono and the runtime hook. Route, queue, direct-Plumber, and authenticated built-stack coverage still needs one coherent acceptance run. | Auth-enabled Hono-to-Plumber table across ordinary users, admins, missing identity, and concurrent principals. | security/principal-forwarding PR to dev; release workflow acceptance. |
| P1-04 | **Implemented, acceptance pending** | Fixed-direction AUC changes and regression coverage remove the old below-0.5 reversal in metrics/ensemble consumers. | Reference oracle for inverted predictions, ties, missing values, constants, and downstream ranking/weights. | science/metric-oracles PR to dev; scientific review in release PR. |
| P1-05 | **Implemented in changed paths, acceptance pending** | MESS lower/upper tails, empirical percentiles, units, predictor retention, scenario separation, and requested masking were repaired in the recovery line. | Known-answer CPU/R parity cases for tails, constants, NA, transforms, both suitability/delta outputs, and exports. | science/mess-validation PR to dev; scientific review and image gate. |
| P1-06 | **Implementation present, acceptance pending** | Current-session checks, conditional refresh consumption, user/session invalidation, and reauthentication migration are present. Full PostgreSQL concurrency and every open stream/revocation path are not yet accepted. | PostgreSQL-backed concurrent refresh/recovery/revoke matrix plus SSE/WebSocket revocation and reconnect tests. | security/session-streams PR to dev; exact-SHA auth matrix. |
| P1-07 | **Implemented on new path, acceptance pending** | Secret rejection, allowlisted execution projection, and sanitized status/manifest/export handling are present on the recovery path. Historical artifacts and every failure/log surface remain an acceptance concern. | Synthetic sentinel through DB, queue, logs, errors, status, CSV/RDS, script, manifest, and downloads; private remediation review if needed. | security/secret-containment PR to dev; release security evidence. |
| P1-08 | **Open** | Current output manifest behavior still has reconstructive/best-effort paths and does not constitute one immutable worker-sealed provenance record with all executed parameters. | Tiny worker run, sealed manifest byte stability after restart/source/path changes, input/output hashes and package/image identity. | science/immutable-provenance PR to dev; research/release gate. |
| P2-09 | **Partial, not deployable-accepted** | Targets packages/workflow groundwork exists, but actual image entrypoints, direct run mapping, and restart/result/cancel ownership are not proven. | Built-image two-species CPU submit/poll/result/cancel/retry/restart with durable ownership. | runtime/targets-lifecycle PR to dev; runtime image acceptance. |
| P2-10 | **Open** | PID/process-local registry and event-origin assumptions do not prove replica-safe identity or cancellation. | Equal-PID containers, owner restart, PID reuse, Redis reconnect, owner routing, and exact-job cancellation. | runtime/execution-identity PR to dev; keep single-replica limit until green. |
| P2-11 | **Open** | Generic proxy retry can replay mutations; durable execution reservation and accepted/lost-response reconnect are not established. | Concurrent duplicate and lost-response tests against durable DB/queue/Plumber boundary. | runtime/execution-identity PR to dev; lifecycle/idempotency release gate. |
| P2-12 | **Open** | Actual GLM fit still needs verified fold-local VIF semantics; ensemble fields/labels still require aligned out-of-fold evaluation and consumer migration. | Known-answer fit-entrypoint test and cross-layer metric schema/report/frontend assertions. | science/validation-semantics PR to dev; scientific reviewer sign-off. |
| P2-13 | **Open** | Script export still has result-shape/argument drift and is not a verified replay contract. | Generate, parse, and execute a tiny offline export from canonical effective configuration. | science/reproducible-export PR to dev; release artifact gate. |
| P2-14 | **Open** | Cache manifest records hashes but fast availability checks do not yet verify current content on the required path. | Same-size tamper, stale, corrupt, and valid-cache cases with verified hash provenance. | science/cache-integrity PR to dev; release data-integrity gate. |
| P2-15 | **Partial** | The recovery line repairs the reviewed action pin and test/mocking baseline, but exact-SHA complete gates, version alignment, governance, and release publication remain unaccepted. | Run all required gates on one exact candidate; reconcile version/changelog/image identity and review policy separately. | release/baseline-and-governance PR to dev; reviewed dev -> main release PR. |

## Disposition rules

No finding is closed by a branch name, test count, merged PR title, or a passing mock. A release claim requires the evidence in the next-gate column on the exact candidate SHA. Findings that depend on production credentials, deployment, governance, or public release remain separately approval-gated.

Original review strengths and rejected hypotheses remain context, not closure evidence. Preserve synthetic-only testing and do not copy private data, credentials, or local workspace details into this ledger.
